# Frontend Technical Assessment

## Notes

The Create React App ESLint configuration was added to `package.json` so TypeScript files are parsed correctly. Without it, false parsing errors triggered a full-screen error overlay that intercepted user interactions and made controls appear unresponsive.
